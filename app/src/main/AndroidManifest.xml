<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nithin.mrp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DeleteConfirmActivity"></activity>
        <activity android:name=".BuyDetailsActivity" />
        <activity android:name=".OrderDetailsActivity" />
        <activity android:name=".adapter.CustomAdapterBuy" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".RemoveActivity" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".AddStockActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>